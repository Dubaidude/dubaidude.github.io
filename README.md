# Tesla Fleet API – Public Key

Dieses Repository enthält den öffentlichen Schlüssel (Public Key) für die Tesla Fleet API App-Registrierung.

Der Key wird von Tesla verwendet, um die Identität der App zu verifizieren.
